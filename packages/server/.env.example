DATABASE_URL=postgres://co:co@localhost:5432/co
# 8080 被宿主机已有服务占用（host 网络），用 7620
PORT=7620
HOST=0.0.0.0
RUNNER_SHARED_TOKEN=dev-runner-token
# openssl rand -hex 32 生成；会话签名与 token 加密的密钥源
AUTH_SECRET=
PUBLIC_URL=http://localhost:7620
